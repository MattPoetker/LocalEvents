<h1>New Listing</h1>

<%= form_for @listing do |e| %>
  <p>
    <%= e.label :name %><br>
    <%= e.text_field :name %>
  </p>
  
 
  <p>
    <%= e.submit %>
  </p>
<% end %>

<%= link_to 'Back', listings_path %>
